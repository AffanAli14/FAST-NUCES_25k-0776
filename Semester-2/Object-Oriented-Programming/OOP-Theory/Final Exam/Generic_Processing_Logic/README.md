2. Generic Processing Logic

    5. The Real Estate App:
    
    Rule: Write a generic utility tool named findBetterProperty. It must accept any two objects, provided they are of the exact same data type.Rule: Inside this utility, call a method .getSquareFootage() on both objects and return the object that has the larger value.

    6. HR Performance Tracker:
    
    Rule: Write a generic utility named getTopPerformer that takes two objects of the exact same type. Inside the function, compare them using their .getKPI() method and return the object with the higher KPI.

    7. E-commerce Discount Engine:
    
    Rule: Write a generic utility named cheaperItem that takes two objects of the exact same type. It should use the .getFinalPrice() method of both objects and return the object that is numerically cheaper.